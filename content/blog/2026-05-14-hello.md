+++
title = "Hello from QTerminator"
date = 2026-05-14
description = "First post on the QTerminator site."
[taxonomies]
tags = ["meta", "release"]
+++

This is the first post on the new QTerminator site.

## Why a Qt port of Terminator

GTK-based Terminator has been the gold standard for split-pane terminals
on Linux for years. QTerminator brings the same workflow — recursive
splits, tab grouping, broadcast input — into a Qt stack so it sits next
to other Qt apps without theme clashes and pulls in fewer dependencies
on KDE/Qt-first distros.

```python
class TerminalGroup:
    """Broadcast input to every terminal sharing this group name."""
    name: str
    members: list[TermWidget]
```

## What's next

- Smoother handling of dragged splits across nested containers
- A profile import path from `~/.config/terminator/config`
- Real screenshots on this page
